//! Progress indicator utilities

// Placeholder for progress utilities
pub struct ProgressBar;

impl ProgressBar {
    pub fn new(_total: u64) -> Self {
        Self
    }

    pub fn set_message(&self, _msg: &str) {}
    pub fn finish(&self) {}
}
